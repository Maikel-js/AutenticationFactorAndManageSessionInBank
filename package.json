{
  "name": "fintechshield-platform",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "prepare": "husky install",
    "lint": "npm --workspace @fintechshield/web run lint",
    "typecheck": "npm --workspace @fintechshield/web run typecheck",
    "test": "npm --workspace @fintechshield/web run test",
    "security:scan": "node ./scripts/security-scan.mjs"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
